# npm 10.9.x arborist crashes ("Cannot read properties of null (reading 'edgesOut')")
# while resolving the vitest 4 / @vitest/browser-playwright / @vitejs/devtools-* peer set.
# legacy-peer-deps bypasses that peer-resolution path. Remove if npm >= 11 is adopted.
legacy-peer-deps=true
